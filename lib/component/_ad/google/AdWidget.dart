import 'package:flutter/cupertino.dart';

class AdWidget extends StatelessWidget {
  final String id;

  const AdWidget({super.key, required this.id});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
